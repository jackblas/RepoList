<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />



    <style name="ListItemTitle" parent="@android:style/TextAppearance.Medium">
        <item name="android:fontFamily">sans-serif-condensed</item>
        <item name="android:textColor">@android:color/black</item>

    </style>

    <style name="ListItemLabel" parent="@android:style/TextAppearance.Small">
        <item name="android:fontFamily">sans-serif-condensed</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:paddingRight">@dimen/label_margin</item>

    </style>

    <style name="ListItemText" parent="@android:style/TextAppearance.Small">
        <item name="android:fontFamily">sans-serif-condensed</item>
    </style>

    <style name="DetailsTitle" parent="@android:style/TextAppearance.Large">
        <item name="android:fontFamily">sans-serif-condensed</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:layout_marginTop">@dimen/text_vertical_margin</item>
        <item name="android:layout_marginBottom">@dimen/text_vertical_margin</item>

    </style>

    <style name="DetailsLabel" parent="@style/ListItemLabel">
        <item name="android:layout_marginBottom">@dimen/text_vertical_margin</item>
    </style>

    <style name="DetailsText" parent="@style/ListItemText">
        <item name="android:layout_marginBottom">@dimen/text_vertical_margin</item>
    </style>

</resources>
